apiVersion: v1
kind: Service
metadata:
  name: stockops-ai
  namespace: stockops
  labels:
    app: stockops-ai
spec:
  type: ClusterIP
  selector:
    app: stockops-ai
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
