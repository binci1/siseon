apiVersion: v1
kind: Service
metadata:
  name: stockops-client-web
  namespace: stockops
  labels:
    app: stockops-client-web
spec:
  type: ClusterIP
  selector:
    app: stockops-client-web
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
